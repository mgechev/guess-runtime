import { PrefixMap, splitter } from './prefix-map';

// describe('PrefixMap', () => {});

describe('splitter', () => {
  it('should work with empty strings', () => {
    expect(splitter('')).toEqual([]);
  });
});
